{
  "name": "lexvoice",
  "version": "1.16.1",
  "description": "Obsidian desktop plugin for recording, transcription, and AI-organized Markdown notes.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "typecheck:core": "tsc -p tsconfig.strict-core.json",
    "check:undefined-symbols": "node scripts/check-undefined-symbols.mjs",
    "build": "npm run check:undefined-symbols && npm run typecheck:core && tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "test": "vitest run",
    "verify": "npm run build && npm test",
    "test:watch": "vitest",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin",
    "transcription",
    "voice-notes",
    "meeting-notes"
  ],
  "author": "Lynnx",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.30",
    "esbuild": "^0.25.0",
    "obsidian": "latest",
    "typescript": "^5.6.3",
    "vitest": "^4.1.8"
  },
  "dependencies": {
    "ws": "^8.21.0"
  }
}
